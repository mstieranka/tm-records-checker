# Roadmap

Planned refactors in order of execution:

- [ ] 1. Rewrite UI using `@shadcn/ui`
- [ ] 2. Migrate database from SQLite to PostgreSQL
- [ ] 3. Move configuration from `config.json` into the database; add initial setup UI (depends on steps 2 and 3)
- [ ] 4. Switch package manager from Bun to `pnpm` (Bun runtime dependency removed by step 3)
