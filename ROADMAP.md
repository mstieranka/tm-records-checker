# Roadmap

Planned refactors in order of execution:

- [ ] 1. Migrate database from SQLite to PostgreSQL
  - [ ] Add migration from SQLite to PostgreSQL
  - [ ] Add Docker setup for PostgreSQL
- [ ] 2. Move configuration from `config.json` into the database; add initial setup UI
- [ ] 3. Switch package manager from Bun to `pnpm`
